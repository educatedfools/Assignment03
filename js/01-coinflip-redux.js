/*
let coinFlip;
let input = prompt("How many flips?");
let inputparsed = parseInt(input);
for(let i = 0; i < inputparsed; i++){
    coinFlip = Math.round(Math.random());
    if(coinFlip === 1){
        console.log('heads!');
    }
    else {
        console.log('tails!');
    }
}
*/